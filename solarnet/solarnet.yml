---
- hosts: all
  vars:
    gateway_group: all
  pre_tasks:
  - include_vars: secrets.yml
  roles:
    - common
    - docker
    - cjdns
    - ipfs
    - ipfs_gateway
    - node_exporter

- hosts: pluto
  pre_tasks:
  - include_vars: secrets.yml
  roles:
  - metrics
