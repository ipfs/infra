---
- hosts: bootstrappers
  become: false
  roles:
    - { role: ssh-keys,
          ssh_user: ubuntu }
    - nginx_conf_bootstrappers
    - { role: geerlingguy.docker,
          docker_install_compose: false }
    - role: python-dependencies
    - role: go-ipfs-deploy-docker
  vars_files:
    - vault.yml
